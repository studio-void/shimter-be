services:
  db:
    image: postgres:15
    restart: always
    container_name: prisma-db
    environment:
      POSTGRES_USER: ${DATABASE_USER}
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD}
      POSTGRES_DB: shimter
    ports:
      - '5432:5432'
    networks:
      - app-network

  app:
    build: .
    container_name: shimter
    ports:
      - '3000:3000'
    depends_on:
      - db
    environment:
      - DATABASE_URL=${DATABASE_URL}
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
